# Codex GUI 项目协作规范

## 适用范围

本文件适用于仓库内所有源码、资源、构建脚本、文档和测试的修改。它约束 Codex 或其他自动化开发代理的工作方式；用户在当前任务中提出的明确要求优先于本文件。

## 版本与变更记录

1. 开始修改前，先读取 `build.gradle.kts` 中的 `version`，将其作为本次变更对应的版本号。
2. 每次修改完成后，必须在 `CHANGELOG.md` 中更新对应版本号的章节：
   - 章节已存在时，在该章节追加简洁的中文条目。
   - 章节不存在时，先创建 `## 版本号 - YYYY-MM-DD` 章节，再添加条目。
   - 条目描述用户可感知的功能、修复、行为变化或构建变化；不要记录纯格式化噪音。
3. 如果本次工作改变了发布版本，必须同时更新 `build.gradle.kts`、`README.md` 的当前版本和 `CHANGELOG.md` 的版本章节，并检查插件元数据是否使用同一版本号。
4. 不要覆盖或重写已有历史记录；保持版本章节按从新到旧排列。

## README 维护

1. 修改用户可见功能、交互、配置、命令、构建方式、兼容性或已知限制时，必须同步维护 `README.md`。
2. README 中的当前版本、能力列表、开发命令、产物路径和已知边界必须与代码及构建脚本一致；删除已经失效的说明。
3. 新增能力优先补充到对应的“当前能力”或“开发”章节，避免重复创建相近章节。
4. README 面向使用者和贡献者，使用简洁中文说明；命令、路径、类名和配置键使用行内代码格式。

## 实现与验证

1. 先阅读相关代码、测试和文档，再进行最小范围修改；不要顺手重构无关模块。
2. 保持现有 Java、HTML、CSS、JavaScript 和 Gradle 风格，不引入未必要的依赖或抽象。
3. 修改 JavaScript 后运行 `node --check src/main/resources/web/app.js`；修改 Java 或构建配置后至少运行 `gradlew.bat test`。
4. 涉及发布包、插件元数据或资源时运行 `gradlew.bat buildPlugin`，并确认 `build/distributions/` 中生成的文件版本正确。
5. 完成前运行 `git diff --check`，检查未提交差异，并在交付说明中列出实际执行过的验证命令及结果。

## 文档与显示文本

1. 面向用户的界面文本使用中文，并保持现有术语一致。
2. 文档中的版本号、命令和路径必须可直接复制使用；不要保留未经验证的示例结果。
3. 注释只解释流程、状态变化或非显然原因，不逐行翻译代码。
