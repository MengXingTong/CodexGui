# Changelog

## Unreleased

## 0.5.0 - 2026-09-04

- 项目正式更名为 `CodeDeck`；构建产物、JetBrains 插件与设置页统一使用新名称，同时保留插件 ID、配置存储和 Bridge 标识以兼容原有安装与用户数据。
- 前端迁移到 TypeScript 与 esbuild，锁定 Node.js 和 npm 依赖；Gradle `check` 现在统一执行类型检查、Vitest、前端打包和 Java 测试。
- Bridge 接收入口收敛为单一 receiver，页面状态由 store/reducer 管理；流式增量使用 `requestAnimationFrame` 按 Session 和消息合并，只更新目标消息节点。
- 长会话改为每批加载 100 条历史消息；Markdown 使用 `marked` 渲染并经 DOMPurify 清洗，阻止回复内容注入脚本。
- Settings 改为不可变运行时快照和枚举化受限值，并增加一次性 schema migration；项目文件搜索迁移到 JetBrains `ProjectFileIndex`，工作区路径与 UTF-8/可执行文件处理统一复用公共工具；ChangeSet 改用当前批量 VFS 事件接口刷新已跟踪路径。
- 移除 JUnit 4 运行时；测试任务排除仅为旧测试框架引入的 IntelliJ `testFramework.jar`，业务测试全部由 JUnit 5 执行。
- 统一插件构建配置、前端包元数据、README 和 Codex 客户端标识中的版本号为 `0.5.0`。
- 新增最小 `ConversationProvider`、Provider capability/health 与统一 `TurnEvent`；Codex/Claude adapter 负责启动、取消及协议归一化，所有回合事件保留 Session、Turn 和 generation 身份。
- `CodexToolWindowPanel` 收敛为 JCEF 创建、依赖组装、页面加载和 dispose；会话控制迁入 Controller，审批交互与注意力通知分别由粗粒度协调组件管理。
- JCEF Bridge 升级为版本化 v1 envelope，统一携带 `requestId/sessionId/turnId/generation` 与独立 payload；Java sealed command/event、TypeScript discriminated union、共享 JSON Schema、fixtures 和预览页使用同一类型清单。
- 未知 Bridge 版本、类型及缺失身份字段现在返回结构化 `protocol.error`；旧 `{action: ...}` 消息只从 Legacy adapter 兼容入口转换。
- 新增 `PendingInteractionRegistry`，按 request、Session、Turn、generation、交互类型和截止时间隔离并清理并发审批与结构化提问。
- Claude 文件修改改由 `PreToolUse` Hook 在 `Write`、`Edit` 与 `NotebookEdit` 执行前同步登记首次基线；项目级 relay 仅监听 loopback，并以 Session、Turn 和 generation 绑定的随机 token 拒绝缺失、畸形、越界及过期请求。
- Claude Code 最低版本调整为 `2.1.210`；Hook relay 无法建立安全基线时会阻止对应文件工具，不再启用全工作区 Snapshot fallback。
- 用 `ConversationChangeTracker` 替换全工作区 Snapshot：首次明确触碰保存 `PRESENT(bytes/hash)` 或 `ABSENT` 基线，摘要按实时 Document/VFS/磁盘内容计算；Accept 清除基线，Revert 恢复首次基线，监听器只刷新已跟踪路径。
- 删除 `WorkspaceSnapshot`、旧 Snapshot 测试和“捕获 Git 忽略文件”设置；修改列表改用摘要行统计，打开 Diff 时才生成 baseline 到实时 current 的详情。
- Codex app-server 引入 `STOPPED/STARTING/READY/STOPPING/FAILED/DISPOSED` 生命周期、专用执行器和 process generation；初始化、普通 RPC、OAuth 分别使用 20 秒、60 秒和 5 分钟超时，断线会清空对应 pending RPC，停止等待 2 秒后强制结束。
- Claude Code 改为项目级服务并以完整 `TurnHandle` 管理进程；取消异常不再包装成执行失败，Codex 生命周期变化不会影响 Claude 回合。
- 引入 Session Kernel，将线程、回合、供应商、消息、附件、队列、用量和忙碌状态统一归属到 `ConversationSession`；后台回调按 Session/Turn/generation 定向更新，不再通过切换活动页签写入状态。
- 关闭会话现在会停止对应 Provider 回合、释放队列与会话状态；取消、关闭及旧 generation 的迟到事件会由 `SessionCoordinator` 丢弃。
- 新增 Codex、Claude 与 Bridge 协议 fixture 及 Conversation ChangeSet 特征测试，固定多 Provider 重构前的关键行为。
- 新增 push/Pull Request 插件验证工作流；业务测试统一使用 JUnit 5，并移除项目显式声明的 JUnit 4 运行时依赖。

## 0.4.4 - 2026-09-03

- Skills 页面新增“个人 / 官方”来源筛选，默认仅显示个人 Skill；Codex 自带的 `openai-bundled`、`openai-primary-runtime` 和 `.system` 路径会被归为官方来源，并在卡片中显示来源标记。
- 新增按 Claude / GPT 渠道分组的供应商配置系统，可添加、编辑、启用和删除自定义 API 服务，并保留两个渠道的本地 CLI 内置配置。
- 自定义 GPT 供应商通过 Codex `model_providers` 启动覆盖接入 Responses 或 Chat Completions API；自定义 Claude 供应商支持认证令牌和 API Key 两种环境变量认证方式。
- 供应商 API 密钥改由 JetBrains PasswordSafe 保存，不进入插件 XML 或网页状态；切换或编辑配置后，旧会话会阻止跨接口继续发送。
- 供应商设置页移除原先重复的 Claude/Codex 大卡片，改为渠道切换栏、供应商列表及紧凑的启用/编辑/删除操作；主页聊天设置保留唯一的渠道切换入口并显示当前供应商。
- 修复暗色主题下供应商“添加”按钮和底部提示文字对比度不足的问题，并修复进入 MCP 页面后设置侧栏被压缩、导航文字隐藏的问题。
- 新增 Claude Code CLI 支持，可在输入区设置切换 GPT / Claude 渠道，并按页签隔离渠道会话。
- Claude Code 使用结构化流式输出续接会话，显示回复、思考与工具记录；模型由 CLI 运行时回填，不增加自定义模型管理。
- 修复 Claude Code 仅维护捕获计数、未实际生成工作区快照的问题；Claude 回合现在会比较前后文件状态，把新增、修改和删除转换为统一变化模型，与 Codex 结构化 diff 共用同一个累计、接受与撤销入口。
- 修复 Claude 基线快照期间停止任务后仍可能启动 CLI，以及运行中停止造成快照重复收尾、与下一条排队消息重叠的问题。
- 任务悬浮框改为加载当前会话的计划、命令和工具执行记录，并显示运行或完成状态。
- 修复输入区设置弹窗字号和控件明显偏大的问题，补充“思考过程”显示开关。
- Diff 右侧改为绑定实际源文件，打开时使用文件最新内容并支持直接编辑；查看 Diff 不会自动接受或撤销修改。
- 修复 Unified Diff 文件头被计入新增/删除统计的问题，并修复状态标签点击后修改面板立即关闭的问题。
- 修复中文输入法组合输入时退格被拦截的问题；修复修改列表因重复事件监听需要多次点击的问题；支持 AI 回复中不带行号的本地文件链接跳转。
- 修复文件引用标签遮挡标签前光标，以及输入框最前方退格误删下一行文件标签的问题。
- 修复连续回合未确认文件修改被后续 diff 覆盖的问题；修改列表现在会累计变更，并保留首次修改前的内容作为撤销基线。
- 优化状态栏、输入区和文件引用标签的紧凑布局，减少编辑区域的额外占用。
- 修复修改捕获列表的单文件接受与撤销按钮缺少样式、持续显示原生灰色按钮的问题。
- 统一插件构建配置、README 和 Codex 客户端标识中的版本号为 `0.4.4`。

## 0.4.2 - 2026-09-01

- 统一插件构建配置、README 和 Codex 客户端标识中的版本号为 `0.4.2`。
- 修复插件启动时页面早于 Codex app-server 完成初始化，导致模型列表和 Skills 请求失败并提示无法写入 app-server 的问题。

## 0.4.1 - 2026-08-29

- 修复聊天输入框在文本末尾需要按两次 `Shift+Enter` 才能看到换行的问题。
- 修复对话未结束时接受文件修改后，下一次 AI 修改到达又被累计 diff 重新加入捕获列表的问题。
- 恢复 AI 回复进行中的消息排队能力，后续输入会按会话顺序自动发送，切换页签不会串线。
- 缩小聊天输入框、状态标签及相关悬浮菜单的文字字号，减少输入区占用空间。
- 修复 AI 回复中的 Windows 文件链接带有 Markdown 转义或 `/E:/` 盘符前缀时无法跳转的问题。
- 连续执行多条命令时合并为带独立滚动区域的批量命令框，减少命令消息对聊天空间的占用。
- 修复跨 IDE 窗口复制已发送消息的文件路径时剪贴板格式丢失的问题，并支持按原顺序恢复混合文字中的文件标签。
- 将插件版本、README、构建配置和 Codex 客户端标识统一更新为 `0.4.1`。
- 插件发行包目标版本更新为 `0.4.1`，产物输出到 `build/distributions/` 便于安装验证。
- 修改捕获列表改为仅显示文件名，完整路径仍可通过悬浮提示查看。
- 修复思考模式从“中”切换到“高”后选择展开栏首次绘制不完整的问题。
- 优化选择器菜单首次绘制稳定性，减少菜单显示时的布局闪动。
- 修复进入设置界面再返回聊天页后，未发送的输入内容丢失的问题。
- 修复多行输入超过可视高度后滚动位置回到顶部，输入光标现在会保持在可视区域内。
- 修复从修改捕获列表打开 Diff 后无法跳转到对应源文件的问题。

## 0.4.0 - 2026-08-27

- 开始将工作区修改捕获改为按会话页签独立维护，文件修改、接受、撤销和捕获计数不再在不同会话之间共享。
- 关闭会话页签或在当前页签开启新对话时，默认确认并清理该会话的待处理修改；后续会话不会再被已关闭会话的修改状态影响。
- 修改事件携带目标会话标识，后台页签的修改列表不会因当前活动页签切换而串联。

## 0.3.5 - 2026-08-27

- “开启新对话”改为复用当前页签，确认后清空线程、消息、附件、文件引用和输入上下文，避免无意义地产生空白页签。
- 新增命令输出的展开/收起详情，命令流式输出按短时间窗口合并刷新，降低高频重绘对界面响应的影响。
- 支持从 Codex 回复中的文件路径链接直接跳转到 JetBrains 编辑器对应的文件、行和列。
- 将任务、子代理和文件修改入口统一为底部状态标签，并保留修改统计和处理入口。
- 命令、文件和权限审批改为异步等待，避免审批对话框阻塞 IDE 主线程。
- 文件拖放结果改为通过输入区标签和附件列表反馈，减少遮挡输入框的提示弹窗；设置与提示词编辑文本框实时保留草稿内容。
- 修复切换页签或返回聊天页后滚动位置未回到底部的问题。

## 0.3.4 - 2026-08-24

- AI 响应进行中时，在聊天底部显示带旋转加载图标、状态文字和已用时间的运行提示。
- 将 `AGENTS.md` 加入 Git 忽略配置，避免本地协作规范文件进入版本控制。
- 彻底移除聊天时间线与输入卡之间容易遮挡内容的任务/修改状态栏及其切换入口。
- 修复输入 `@` 后文件候选栏反复重绘聊天框、打断文字选择的问题，候选搜索改为局部更新。
- 修复从 `@` 文件候选中选择项目文件后只插入普通路径文本的问题，现在会在原位置生成可操作的文件标签。
- 将修改捕获改为仅处理 Codex app-server 报告的文件 diff，不再扫描整个工作区，避免 Unreal 项目发送消息时耗尽 Rider JVM 堆内存。
- 将原本仅保存到当前项目的提示词配置迁移为用户级共享配置，切换项目后无需重复填写。
- 在 `AGENTS.md` 记录本机 JDK 21 路径，后续 Gradle 构建可直接复用，减少重复查找。
- Windows 下启动 Codex 时优先使用用户级 `~/.codex` 安装目录，避免 PATH 上的旧版 `codex` 抢占连接。
- 修复插件硬编码 OpenAI provider 导致用户级 `config.toml` 的自定义 provider 和 `base_url` 被忽略的问题。
- 启动 app-server 时显式继承 `CODEX_HOME`，确保插件与 Codex 应用读取同一份配置和认证文件。
- Windows 下优先使用包含配套 `codex-code-mode-host.exe` 的 app-server 运行时，修复 Code Mode host 路径错误。
- 顶部新增“开启新对话”按钮；当前会话已有消息时会先弹出确认窗口，再创建新的空白页签。
- 恢复聊天输入区上方的修改捕获栏，可查看文件统计并逐项或批量接受、撤销修改。
- 移除聊天中的推理摘要框及对应开关，回复正文直接按普通消息显示。
- 命令执行改为单行省略显示，右侧圆点以绿色、红色分别表示成功和失败，运行中显示灰色状态。
- 修复 AI 回合中的“命令”消息标题误覆盖会话页签名称的问题。
- 修复项目文件引用仍按相对路径解析的问题；所有文件引用现在统一使用 `@` 加完整绝对路径传递。

## 0.3.3 - 2026-08-23

- 重新整理亮色主题颜色体系，提高正文、次级文字、图标、边框、禁用状态和占位文本的对比度，并统一聊天区、弹层、设置、Skills、MCP 与模态窗口的亮色交互状态。
- 增加项目级 `AGENTS.md` 协作规范并纳入版本控制，要求版本变更同步维护 CHANGELOG 和 README，并明确修改后的验证流程。
- 修复亮色主题下页签右键菜单和任务/修改状态面板显示深色背景的问题。
- 修复空白页签重命名无效的问题，新建页签自动使用递增序号标题以区分多个会话。
- 修复长流式回复发送过程中反复序列化完整消息导致 IDE Java heap space 的问题，改为仅传输每次新增文本。
- 将多会话页签移动到工具窗顶端，增加 CC-Gui 风格右键菜单、页签列表、批量关闭、前后切换、重命名、导出、搜索、左右移动及键盘快捷键。
- 修复文件引用发送顺序：标签现在嵌入对应文字位置，并向 Codex 和会话摘要传递完整绝对路径，不再统一追加为 `@文件名`。
- 文件引用改用稳定 ID 跟踪，修复删除或剪切中间标签后再次插入发生错位，并确保同路径的多个标签可被精确删除和重排。
- 修复输入聊天框内容超过自动高度上限后无法滚动查看的问题。
- 支持在聊天输入框内直接拖动文件引用标签调整顺序，并同步到后端引用列表。
- 修复首个文件标签无法退格删除、混合选区输入法被打断和旧文本残留问题；标签拖动与项目树文件拖放显示明确落点，并为标签右键菜单增加剪切。
- 增加 GitHub Actions Gradle 发布流程：发布 GitHub Release 后可自动执行 `publishPlugin`，也支持手动触发。
- 规范 README 的内容结构，明确项目尚未正式发布且仍在开发中，并移除维护者专用的发布流程说明。

## 0.3.2 - 2026-08-23

- 项目采用 MIT License 开源，并在源码仓库与插件发行包中保留 CC GUI 的原始版权和许可声明。
- 增加多会话页签，可在同一个 JetBrains 工具窗中并行运行多个独立 Codex 回合。
- 页签分别保存线程、消息、输入草稿、附件、文件引用和运行状态，后台页签事件按会话路由。
- 并行回合共享工作区修改捕获周期，单个任务完成不会提前停止其它运行中任务的修改扫描。
- 收紧审批模式展开菜单的宽度、行高、间距与字号，减少工具窗内的放大感。
- 将 Agent 身份切换入口移至输入区身份标签，并精简聊天设置浮层。
- 修复文件引用标签无法选择复制，以及拖入或粘贴的标签总是插到输入框开头的问题。

## 0.3.1 - 2026-08-23

- 完成 Codex CLI 原生会话创建、恢复、历史浏览和流式响应。
- 支持模型、推理强度、沙箱、审批策略、命令执行审批和中断。
- 支持结构化提问、文本/文件/图片输入、编辑器选区、项目树拖拽和 Skills 管理。
- 支持会话事件展示、工作区修改捕获、IDE Diff、保留/撤销、收藏、搜索、重命名和 Markdown 导出。
- 完成 CC-Gui 风格的单列会话界面与设置侧栏。
- 增加 Windows 下 Codex Desktop CLI 自动发现，并过滤无法由普通进程启动的 WindowsApps 副本。
